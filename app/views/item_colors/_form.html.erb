<%= form_for(@item_color) do |f| %>
  <% if @item_color.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item_color.errors.count, "error") %> prohibited this item_color from being saved:</h2>

      <ul>
      <% @item_color.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :color_id %><br>
    <%= f.number_field :color_id %>
  </div>
  <div class="field">
    <%= f.label :item_id %><br>
    <%= f.number_field :item_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
